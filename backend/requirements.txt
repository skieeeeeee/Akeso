# MediKiosk — runtime dependencies
#
# What the API needs to serve traffic. Nothing here is optional and nothing
# here is a test tool.
#
#   pip install -r requirements.txt
#
# See also:
#   requirements-ocr.txt  — reading uploaded documents (large, optional)
#   requirements-dev.txt  — running the test suite

# --- Web framework ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Validation / settings -------------------------------------------------
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database --------------------------------------------------------------
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Auth ------------------------------------------------------------------
pyjwt==2.10.1

# --- HTTP client, for the AI provider abstraction --------------------------
# Only used when AI_PROVIDER=grok. Kept in the runtime set because the
# provider module imports it at startup to decide whether it is available.
httpx==0.28.1

# The visit handoff QR code. Pure Python, and rendered as SVG so it needs no
# image library — which keeps it out of the optional OCR extras.
qrcode==8.0
